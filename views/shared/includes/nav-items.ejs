<ul class="nav-items">
  <% if (!locals.isAdmin) { %>
  <li><a href="/">Shop</a></li>
  <li>
    <a href="/cart"
      >Cart <span class="badge"><%= locals.cart.totalQuantity %></span></a
    >
  </li>
  <% } %> <% if (locals.isAuth && !locals.isAdmin) { %>
  <li><a href="/orders">Orders</a></li>
  <li>
    <p
      id="manageproduct"
      style="text-decoration: none; color: var(--color-primary-400);padding: var(--space-2) var(--space-4);
      border-radius: var(--border-radius-small);cursor: pointer;"
    >
      Manage Products
    </p>
  </li>
  <% } %>
  <!-- <input type="text" name="" id="" value=> -->
  <% if (locals.isAdmin) { %>
  <li><a href="/admin/products">Manage Products</a></li>
  <li><a href="/admin/orders">Manage Orders</a></li>
  <% } %>
  <!-- <% if (locals.isAuth && !locals.isAdmin) { %>
    <li><a href="/admin/products">Add a Product</a></li>
  <% } %> -->

  <% if (!locals.isAuth) { %>
  <li><a href="/signup">Signup</a></li>
  <li><a href="/login">Login</a></li>
  <% } %> <% if (locals.isAuth) { %>
  <li>
    <form action="/logout" method="POST">
      <input type="hidden" name="_csrf" value="<%= locals.csrfToken %>" />
      <button>Logout</button>
    </form>
  </li>
  <% } %>
</ul>

<script type="text/javascript">
  $(document).ready(function () {
    $("#manageproduct").click(function () {
      window.location.href = "/manage-product?uid=<%= locals.uid %>";
    });
  });
</script>
